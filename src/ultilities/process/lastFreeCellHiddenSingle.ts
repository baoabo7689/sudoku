import { SudokuGrid } from "@/models/SudokuGrid";

export function doLastFreeCellHiddenSingle(grid: SudokuGrid): boolean {
  let isUnchanged = true;

  // same as preScanningCrossHatching
  // then doNakedSingle
  
  return isUnchanged;
}
