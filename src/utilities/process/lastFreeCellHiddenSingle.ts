import { SudokuGridModel } from '@/models/SudokuGridModel';

export function doLastFreeCellHiddenSingle(grid: SudokuGridModel): boolean {
  let isUnchanged = true;

  // same as preScanningCrossHatching
  // then doNakedSingle

  return isUnchanged;
}
